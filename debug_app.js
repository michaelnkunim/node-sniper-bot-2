
module.exports = {
    run
}

async function run() {
    setTimeout(() => {
        // ['ENC_32BIT_PASS', 'ENC_KEY', 'INITIALIZATION_VECTOR', 'HMAC_KEY'].forEach(key => {
        //     console.log('-------', key, global.appConfig[key]);
        // });
    }, 500);

}

